{
  "name": "edu-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI智学助手 - Web + 小程序 + 微信云开发后端",
  "scripts": {
    "dev:web": "cd apps/web && node server.js",
    "install:web": "cd apps/web && npm install",
    "install:tutor": "cd cloudfunctions/tutor && npm install",
    "install:detect": "cd cloudfunctions/detect-subject && npm install",
    "install:upload": "cd cloudfunctions/upload-question-image && npm install",
    "install:all": "npm run install:web && npm run install:tutor && npm run install:detect && npm run install:upload"
  }
}
